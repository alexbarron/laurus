Hello,

<%= @app_invitation.inviter.name %> has invited you to be a <%= @app_invitation.admin? ? "admin" : "read-only" %> team member on developer app <%= @app_invitation.developer_app.name %>.

Follow the link below to accept the invitation.

<%= button_to "Accept Invitation", accept_app_invitation_url(developer_app_id: @app_invitation.developer_app.id, id: @app_invitation.id), method: :get, class: 'btn btn-success' %>